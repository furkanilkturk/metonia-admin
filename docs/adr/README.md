# Architecture decision records

Architecture decision records capture durable Metonia Admin decisions, their context, tradeoffs, and consequences. Time-sensitive ecosystem facts belong in `docs/research/` instead.

Planned first decisions cover the Bun monorepo, client/server/shared boundaries, components/views/pages composition, generator recipes, the shared registry, data patterns, UI adapters, database boundaries, and Docker.

